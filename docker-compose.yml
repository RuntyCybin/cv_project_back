version: "3.9"

services:
  experiencias:
    build:
      context: ./experiencias
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
